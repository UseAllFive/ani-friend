const AniConfig = {
    initialDelay: 0.1,
    // An element appears at this time * its order
    delaySpeed: 0.1,
    moveDistance: 20,
    inViewTriggerPercent: 0.4,
    speed: 1.5,
}

module.exports = AniConfig
