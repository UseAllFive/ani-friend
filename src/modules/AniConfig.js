export const AniConfig = {
    initialDelay: 0.1,
    // An element appears at this time * its order
    delaySpeed: 0.2,
    moveDistance: 40,
    zoomScale: 1.1,
    inViewTriggerPercent: 0.7,
    speed: 1,
}
